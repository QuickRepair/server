//
// Created by gaojian on 19-4-11.
//

#ifdef ENV_TEST

#include <gtest/gtest.h>
#include <string>
#include "Configuration/Configure.h"

using std::string;

TEST(ConfigureParseTest, parse)
{
	/// An empty test body
	/*
	Configure configure;
	EXPECT_EQ(string("http://10.42.0.1"), configure.ip());
	EXPECT_EQ(12345, configure.port());
	EXPECT_EQ("http://10.42.0.1:12345", configure.listenOn());
	EXPECT_EQ("localhost", configure.databaseIp());
	EXPECT_EQ(3306, configure.databasePort());
	EXPECT_EQ("HAR", configure.databaseName());
	EXPECT_EQ("root", configure.databaseUserName());
	 */
}
#endif